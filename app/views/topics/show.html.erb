<div class="row text-center">
  <h1><%= @topic.title %></h1>
  <div>
    <%= link_to "All Topics", topics_path(@topic), class: 'btn btn-success' %>
    <%= link_to "Edit", edit_topic_path(@topic), class: 'btn btn-success' %>
    <%= link_to "Delete Topic", @topic, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this topic?'} %>
    <%= link_to "New Bookmark", new_topic_bookmark_path(@topic), class: 'btn btn-success' %>
  </div><br>
  <div>
    <% @topic.bookmarks.each do |bookmark| %>
      <%= link_to bookmark.url, topic_bookmark_path(@topic, bookmark) %>
    <% end %>
  </div>
</div>
