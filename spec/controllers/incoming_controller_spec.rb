require 'rails_helper'

RSpec.describe IncomingController, type: :controller do
  describe "POST create" do
  # it "should test if user is found" do
  # end
  # it "increases the number of user by 1" do
  # expect{ post :create, user_id: my_user.id}.to change(User,:count).by(1)
  # end
  # it "should test if topic is found" do
  # end
  # it "increases the number of topic by 1" do
  # expect{ post :create, topic: {title: RandomData.random_sentence}.to change(Topic,:count).by(1)
  # end
  # it "increases the number of bookmark by 1" do
  # expect{post :create, topic_id: my_topic.id, bookmark: {url: RandomData.random_url}}.to change(Bookmark,:count).by(1)
  # end
  end
end
